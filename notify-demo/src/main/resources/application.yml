server:
  port: 8080

notify:
  channels:
    email:
      host: localhost
      port: 3025
      from: noreply@notifyhub.io
      from-name: NotifyHub Demo
      tls: false
  retry:
    max-attempts: 3
    strategy: exponential
