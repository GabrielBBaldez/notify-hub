<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.gabrielbbaldez</groupId>
        <artifactId>notify-hub-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>notify-mcp</artifactId>
    <name>NotifyHub MCP Server</name>
    <description>MCP server exposing NotifyHub notification tools for AI assistants (Claude, Cursor, etc.).</description>

    <properties>
        <mcp-sdk.version>0.18.1</mcp-sdk.version>
        <maven.deploy.skip>false</maven.deploy.skip>
    </properties>

    <dependencies>
        <!-- NotifyHub Spring Boot starter (auto-configures all channels) -->
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-spring-boot-starter</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- All channel modules -->
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-email</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-sendgrid</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-sms</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-slack</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-telegram</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-discord</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-teams</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-push-firebase</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-webhook</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-websocket</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-google-chat</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-twitter</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-linkedin</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-notion</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-twitch</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-youtube</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-instagram</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-tiktok-shop</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-facebook</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-whatsapp</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-aws-sns</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-mailgun</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.gabrielbbaldez</groupId>
            <artifactId>notify-pagerduty</artifactId>
        </dependency>

        <!-- MCP Java SDK (core — STDIO transport) -->
        <dependency>
            <groupId>io.modelcontextprotocol.sdk</groupId>
            <artifactId>mcp</artifactId>
            <version>${mcp-sdk.version}</version>
        </dependency>

        <!-- Spring Boot (headless, no web server) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- Jackson (required by MCP SDK) -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.sonatype.central</groupId>
                <artifactId>central-publishing-maven-plugin</artifactId>
                <configuration>
                    <skipPublishing>true</skipPublishing>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <executions>
                    <execution>
                        <goals><goal>repackage</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- GraalVM Native Image profile — produces a standalone executable -->
    <profiles>
        <profile>
            <id>native</id>
            <dependencies>
                <dependency>
                    <groupId>org.graalvm.buildtools</groupId>
                    <artifactId>graalvm-reachability-metadata</artifactId>
                    <version>0.10.3</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <version>0.10.3</version>
                        <extensions>true</extensions>
                        <executions>
                            <execution>
                                <id>build-native</id>
                                <goals><goal>compile-no-fork</goal></goals>
                                <phase>package</phase>
                            </execution>
                        </executions>
                        <configuration>
                            <imageName>notifyhub-mcp</imageName>
                            <mainClass>io.notifyhub.mcp.NotifyMcpServer</mainClass>
                            <buildArgs>
                                <buildArg>--no-fallback</buildArg>
                                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
                            </buildArgs>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <version>${spring-boot.version}</version>
                        <configuration>
                            <image>
                                <builder>paketobuildpacks/builder-jammy-tiny:latest</builder>
                            </image>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
